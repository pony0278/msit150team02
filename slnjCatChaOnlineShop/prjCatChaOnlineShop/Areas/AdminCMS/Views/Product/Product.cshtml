@model prjCatChaOnlineShop.Models.ViewModels.CproductsTotal

@{
    ViewData["Title"] = "Product";
    Layout = "~/Areas/AdminCMS/Views/Shared/CMS_Layout.cshtml";
}


@section Styles{
    <style>
        /* 原始圖片的視窗 */
        .image-popup {
            display: none;
            position: absolute;
            top: 0;
            left: 150px;
            /* 將視窗放在圖片右側 */
            /* max-width: 100%; */
            /* 視窗寬度 */
            background-color: #aaa;
            padding: 10px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.3);
            z-index: 1;
        }

            .image-popup img {
                width: 300px;
            }

        /* 縮略圖片的樣式 */
        .thumbnail-image {
            /* position: relative; */
            cursor: pointer;
            /* 將鼠標指針設置為手型以指示圖片可懸停 */
        }

        .test {
            position: relative;
        }

        .form-select {
            width: 200px;
            height: 40px;
        }

    </style>
}

<div class="middle-content">

    <div class="row align-items-center">
        <div class="col-6">
            <h2 class="mt-3">商城產品管理</h2>
        </div>
        <div class="col-6 text-end mt-3">
            <button class="btn btn-primary" data-bs-toggle="modal" href="#addProductModalToggle" role="button">新增商品</button>
        </div>
    </div>
    <!---新增產品 modal-->
    <div class="modal fade" id="addProductModalToggle" aria-hidden="true"
         aria-labelledby="exampleModalToggleLabel" tabindex="-1">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalToggleLabel">新增商品</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"
                            aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form>
                        <div class="form-group">
                            <input type="hidden" class="form-control" id="CreateID" name="ProductId"
                                   disabled readonly>
                        </div>

                        <div class="form-group">
                            <label>名稱：</label>
                            <input type="text" class="form-control" id="CreateName" name="ProductName">
                        </div>

                        <div class="form-group">
                            <label>描述：</label>
                            <input type="text" class="form-control" id="CreateDescription" name="ProductDescription">
                        </div>

                        <div class="form-group">
                            <label>價格：</label>
                            <input type="text" class="form-control" id="CreatePrice" name="ProductPrice">
                        </div>

                        <div class="form-group">
                            <label>商品類別：</label>
                            <select class="form-control" id="CreateCategory" name="ProductCategory">
                                <option value="ProductCatgory1">零食</option>
                                <option value="ProductCatgory2">玩具</option>
                            </select>
                        </div>

                        <div class="form-group">
                            <label>剩餘數量：</label>
                            <input type="text" class="form-control" id="CreateRemainingQuantity" name="RemainingQuantity">
                        </div>

                        <div class="form-group">
                            <label>上架日期：</label>
                            <input type="date" class="form-control" id="CreateReleaseDate" name="ReleaseDate">
                        </div>

                        <div class="form-group">
                            <label>尺寸：</label>
                            <input type="text" class="form-control" id="CreateSize" name="Size">
                        </div>

                        <div class="form-group">
                            <label>重量：</label>
                            <input type="text" class="form-control" id="CreateWeight" name="Weight">
                        </div>

                        <div class="form-group">
                            <label>供應商：</label>
                            <input type="text" class="form-control" id="CreateSupplier" name="Supplier">
                        </div>

                        <div class="form-group">
                            <label>是否停售：</label>
                            <select class="form-control" id="CreateDiscontinued" name="Discontinued">
                                <option value="是">是</option>
                                <option value="否">否</option>
                            </select>
                        </div>

                        <div class="col-md-12">
                            <label>圖片</label>
                            <input type="file" class="form-control" id="CreateImage" name="ShopProductImageTable">
                        </div>

                        <button id="CreateProduct" name="CreateProduct" class="btn btn-danger btn-block"
                                style="width: 100%;">
                            儲存
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <!---新增產品 modal-->
    <!---篩選上架日期-->
    <div class="container-fluid mt-4 mb-3">
        <div class="row align-items-center">
            上架日期區間：<input type="text" class="form-control w-25" name="dates" />
        </div>
    </div>
    <!---篩選上架日期-->
    <!---篩選類別/是否停售-->
    <div class="container-fluid mt-4 mb-3">
        <div class="row align-items-center">
            類別篩選：
            <div class="col d-flex justify-content-between">
                <select class="form-select" id="CpayList">
                    <option value="" selected disabled>請選擇類別</option>
                    <option value="">全部訂單</option>
                    <option value="食品">食品</option>
                    <option value="零食">零食</option>
                </select>
            </div>
        </div>

        @*        <div class="row align-items-center">
        停售狀態篩選：
        <div class="col d-flex justify-content-between">
        <select class="form-select" id="DpayList">
        <option value="" selected disabled>請選擇狀態</option>
        <option value="">全部訂單</option>
        <option value="是">是</option>
        <option value="否">否</option>
        </select>
        </div>
        </div>
        </div>*@
        <!---篩選類別-->

        <div class="container-fluid mt-4">
            <div class="row">
                <div class="col-12">
                    <!--表格-->
                    <table id="Product-table" class="table table-striped nowrap" style="width:100%">
                        <thead>
                            <tr>
                                <th>序號</th>
                                <th>商品名稱</th>
                                <th>描述</th>
                                <th>價格</th>
                                <th>剩餘數量</th>
                                <th>類別</th>
                                <th>圖片</th>
                                <th>上架日期</th>
                                <th>尺寸</th>
                                <th>重量</th>
                                <th>供應商</th>
                                <th>是否停售</th>
                                <th>編輯/刪除</th>
                            </tr>
                        </thead>
                        <tbody>
                        </tbody>
                        <!--編輯彈出視窗-->
                        <div class="modal fade" id="EditProductModalToggle" aria-hidden="true"
                             aria-labelledby="EditProductModalToggleLabel" tabindex="-1">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h5 class="modal-title" id="EditProductModalToggleLabel">編輯 Edit</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal"
                                                aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body">
                                        <!--內容表單-->
                                        <div class="row">
                                            <div class="col-md-6">
                                                <label>ID</label>
                                                <input type="text" id="editor-id" name="ProductId" class="form-control" readonly disabled>
                                            </div>
                                            <div class="col-md-6">
                                                <label>名稱</label>
                                                <input type="text" id="editor-name" class="form-control" name="ProductName">
                                            </div>
                                            <div class="col-md-6">
                                                <label>描述</label>
                                                <textarea id="editor-description" class="form-control" name="ProductDescription"></textarea>
                                            </div>
                                            <div class="col-md-6">
                                                <label>價格</label>
                                                <input type="text" id="editor-price" class="form-control" name="ProductPrice">
                                            </div>
                                            <div class="col-md-6">
                                                <label>剩餘數量</label>
                                                <input type="text" id="editor-remainingquantity" class="form-control" name="RemainingQuantity">
                                            </div>
                                            <div class="col-md-6">
                                                <label>商品類別</label>
                                                <select class="form-control" id="editor-category" name="ProductCategory">
                                                    <option selected>請選擇</option>
                                                    @foreach (var category in Model.ProductCategories)
                                                    {
                                                        <option value="@category.ProductCategoryId">@category.CategoryName</option>
                                                    }
                                                </select>
                                            </div>
                                            <div class="col-md-6">
                                                <label>上架日期</label>
                                                <input type="datetime-local" id="editor-releasedate" class="form-control" name="ReleaseDate">
                                            </div>
                                            <div class="col-md-6">
                                                <label>下架日期</label>
                                                <input type="datetime-local" id="editor-offdate" class="form-control" name="offDate">
                                            </div>
                                            <div class="col-md-6">
                                                <label>尺寸</label>
                                                <input type="text" id="editor-size" class="form-control" name="Size">
                                            </div>
                                            <div class="col-md-6">
                                                <label>重量</label>
                                                <input type="text" id="editor-weight" class="form-control" name="Weight">
                                            </div>
                                            <div class="col-md-6">
                                                <label>折扣</label>
                                                <input type="text" id="editor-discount" class="form-control">
                                            </div>
                                            <div class="col-md-6">
                                                <label>供應商</label>
                                                <input type="text" id="editor-supplier" class="form-control" name="Supplier">
                                            </div>
                                            <div class="col-md-6">
                                                <label>是否停售</label>
                                                <input class="form-check-input" type="checkbox" value="" id="editor-discontinued" name="Discontinued" />
                                            </div>
                                            <div class="col-md-6">
                                                <label>是否上首頁</label>
                                                <input class="form-check-input" type="checkbox" value="" id="editor-pinToShop" name="pinToShop" />
                                            </div>
                                            <div class="col-md-12">
                                                <label>圖片1</label>
                                                <input type="file" class="form-control" id="editor-image" name="ShopProductImageTable">
                                            </div>
                                            <div class="col-md-12">
                                                <label>圖片2</label>
                                                <input type="file" class="form-control" id="editor-image2" name="ShopProductImageTable">
                                            </div>
                                            <div class="col-md-12">
                                                <label>圖片3</label>
                                                <input type="file" class="form-control" id="editor-image3" name="ShopProductImageTable">
                                            </div>
                                            <button type="submit" class="mt-3 btn btn-primary btn-block" name="submitButton-edior" id="submitButton-edior"
                                                    style="width: 100%;">
                                                儲存
                                            </button>
                                        </div>
                                        <!--內容表單-->
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!--編輯彈出視窗-->
                    </table>
                    <!--表格-->
                </div>
            </div>
        </div>

    </div>

    @section Scripts{
        @*引用日期插件*@
        <script type="text/javascript" src="https://cdn.jsdelivr.net/momentjs/latest/moment.min.js"></script>
        <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.min.js"></script>
        <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.css" />

        <script>
            // 查看json結構
            $(document).ready(function () {
                $.ajax({
                    url: '@Url.Action("LoadDatatable", "Product")',
                    type: 'GET',
                    success: function (data) {
                        console.log(data);
                    },
                    error: function (error) {
                        console.log(error);
                    }
                });
            });

            //初始化表格==================================================
            $(document).ready(function () {
                var table = $('#Product-table').DataTable({
                    responsive: true,
                    "paging": true,
                    "ajax": {
                        "url": "@Url.Action("LoadDatatable", "Product" , new{ area="AdminCMS"})",
                        type: 'GET',
                    },
                    //載入欄位資料庫資料==========================================
                    "columns": [
                        { "data": "productId" },
                        { "data": "productName" },
                        { "data": "productDescription" },
                        { "data": "productPrice" },
                        { "data": "remainingQuantity" },
                        { "data": "productCategory" },
                        { "data": "shopProductImageTable" },
                        { "data": "releaseDate" },
                        { "data": "size" },
                        { "data": "weight" },
                        { "data": "supplier" },
                        { "data": "discontinued" },
                        {
                            "data": "productId",
                            "render": function (data, type, row) {
                                var deleteUrl = '@Url.Action("Delete", "Product", new { area = "AdminCMS", id = "productId" })';
                                deleteUrl = deleteUrl.replace('productId', row.productIdd);
                                return `<button class="edit-button" data-id="${row.productId}" data-bs-toggle="modal" data-bs-target="#EditProductModalToggle"><i class="fas fa-edit"></i></button> |
                                                                <a href="${deleteUrl}" onclick="return confirm('確定要刪除嗎?')"><i class="fas fa-trash-alt"></i></a>`;
                            }
                        }
                    ]
                });


                //篩選類別==============================================
                // 在這裡指定需要過濾的列，商品類別為第六列->索引5
                columnDefs: [
                    { targets: 5, searchable: true }
                    //{ targets: 11, searchable: true }
                ]
            });

            // 綁定select元素的change事件，當選擇條件改變時，重新過濾表格資料
            $('#CpayList').on('change', function () {
                var filterOption1 = $(this).val();

                // 使用DataTable的column().search()方法進行過濾
                table.column(5).search(filterOption1).draw();
            });

            //$('#DpayList').on('change', function () {
            //    var filterOption2 = $(this).val();
            //    table.column(11).search(filterOption2).draw();
            //});

            ;

            //圖片預覽-當頁面載入完成後執行==================================

            document.addEventListener("DOMContentLoaded", function () {
                // 獲取所有包含縮略圖片和視窗的元素
                const thumbnailImages = document.querySelectorAll(".thumbnail-image");
                const imagePopups = document.querySelectorAll(".image-popup");

                // 對每個圖片進行事件處理
                thumbnailImages.forEach((thumbnailImage, index) => {
                    // 當滑鼠懸停在圖片上時
                    thumbnailImage.addEventListener("mouseover", function () {
                        // 顯示對應的原始圖片的視窗
                        imagePopups[index].style.display = "block";
                    });

                    // 當滑鼠移開圖片時
                    thumbnailImage.addEventListener("mouseout", function () {
                        // 隱藏對應的原始圖片的視窗
                        imagePopups[index].style.display = "none";
                    });
                });
            });

            //日期篩選器==============================
            $('input[name="dates"]').daterangepicker();
            $(document).ready(function () {
                var table = $('#salon-table').DataTable({
                    responsive: true,
                });
            });

            //刪除按鈕================================
            function userDelete() {
                Swal.fire({
                    title: '確定要刪除嗎?',
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#3085d6',
                    cancelButtonColor: '#d33',
                    confirmButtonText: 'Yes, delete it!'
                }).then((result) => {
                    if (result.isConfirmed) {
                        Swal.fire(
                            'Deleted!',
                            'Your file has been deleted.',
                            'success'
                        )
                    }
                })
            }

            //編輯按鈕==============================================
            $('#Product-table').on('click', '.edit-button', function () {
                let id = $(this).data('id')
                $.ajax({
                    url: '/AdminCMS/Product/EditShopProducts?id=' + id,
                    type: 'GET',
                    success: function (response) {
                        if (response.success) {
                            $('#editor-id').val(response.data.productId);
                            $('#editor-name').val(response.data.productName);
                            $('#editor-description').val(response.data.productDescription);
                            $('#editor-price').val(response.data.productPrice);
                            $('#editor-remainingquantity').val(response.data.remainingQuantity);
                            $('#editor-category').val(response.data.productCategoryId);
                            $('#editor-releasedate').val(response.data.releaseDate);
                            $('#editor-size').val(response.data.size);
                            $('#editor-weight').val(response.data.weight);
                            $("#editor-discount").val(response.data.discount);
                            $('#editor-supplier').val(response.data.supplier.companyName);
                            $('#editor-discontinued').prop(response.data.discontinued);
                            $('#editor-image').val(response.data.shopproductImagetable);
                            $('#editor-pinToShop').prop("checked",response.data.pushToShop)
                            $('#editor-offdate').val(response.offDay)
                            console.log(response.data)
                        } else {
                            alert(response.message);
                        }
                    }
                });
            });
            let TypeEditor;
            $('#editor-category').on('change', function () {
                TypeEditor = parseInt($(this).val(), 10);
            })
            //儲存按鈕==============================
            $('#submitButton-edior').click(function () {
                var id = $('#editor-id').val();
                var name = $('#editor-name').val()
                var description = $('#editor-description').val()
                var price = $('#editor-price').val()
                var remainingquantity = $('#editor-remainingquantity').val()
                var releasedate = $('#editor-releasedate').val()
                var size = $('#editor-size').val()
                var offday = $("#editor-offdate").val()
                var weight = $('#editor-weight').val()
                var discount = $("#editor-discount").val()
                var supplier = $('#editor-supplier').val()
                var discontinued = $('#editor-discontinued').is(":checked")
                var pinToShop = $('#editor-pinToShop').is(":checked")
                var fileInput = $('#editor-image')[0];
                var file = fileInput.files[0];
                var fileInput2 = $('#editor-image')[0];
                var file2 = fileInput2.files[0];
                var fileInput3 = $('#editor-image')[0];
                var file3 = fileInput3.files[0];
                console.log(id)
                console.log(name)

                var formData = new FormData();
                formData.append("ProductId", id);
                formData.append("Productname", name);
                formData.append("Productdescription", description);
                formData.append("ProductPrice", price);
                formData.append("RemainingQuantity", remainingquantity);
                formData.append("ProductCategoryId", TypeEditor);
                formData.append("ReleaseDate", releasedate);
                formData.append("Size", size);
                formData.append("Weight", weight);
                formData.append("SupplierId", supplier);
                formData.append("Discontinued", discontinued);
                formData.append("OffDay", offday)
                formData.append("Discount", discount)
                formData.append("Image", file)
                formData.append("PushToShop", pinToShop)

                $.ajax({
                    url: "@Url.Action("EditShopProducts","Product" , new { area="AdminCMS" })",
                    type: "POST",
                    data: formData,
                    processData: false,
                    contentType: false,
                    success: function (response) {
                        alert("Content saved successfully!");
                        window.location.reload();
                    }
                });
            });

            //新增商品================================================
            $('#CreateProduct').click(function () {

                var id = $('#CreateID').val();
                var name = $('#CreateName').val();
                var description = $('#CreateDescription').val();
                var price = $('#CreatePrice').val();
                var remainingquantity = $('#Createremainingquantity').val();
                var category = $('#Createcategory').val();
                var releasedate = $('#Createreleasedate').val();
                var size = $('#Createsize').val();
                var weight = $('#Createweight').val();
                var supplier = $('#Createsupplier').val();
                var discontinued = $('#Creatediscontinued').val();
                var image = $('#Createimage').val();

                // 檢查必填欄位是否有值
                if (!id || !name || !description || !price || !remainingquantity || !category || !releasedate || !size || !weight || !supplier || !discontinued || !image) {
                    Swal.fire({
                        icon: 'error',
                        title: '錯誤',
                        text: '請填寫所有必填欄位。'
                    });
                    return; // 不執行後續動作
                }

                // 檢查帳號是否已存在或者isVaild只要是false就return
                if (CheckAccResult == true || isVaild == false) {
                    return; // 不執行後續動作
                }

                var newProduct = {
                    id: id,
                    name: name,
                    description: description,
                    price: price,
                    remainingquantity: remainingquantity,
                    category: category,
                    releasedate: releasedate,
                    size: size,
                    weight: weight,
                    supplier: supplier,
                    discontinued: discontinued,
                    image: image
                };

                try {
                    $.ajax({
                        url: '/AdminCMS/Product/CreateProduct',
                        type: 'POST',
                        data: { newProduct: newProduct },
                        success: function (response) {
                            // console.log(result)
                            if (response.success) {
                                Swal.fire({
                                    icon: 'success',
                                    title: '新增成功',
                                    text: response.message
                                });

                                // 關閉modal
                                $('#addProductModalToggle').modal('hide');

                                // 重新整理dataTable
                                $('#Product-table').DataTable().ajax.reload(); // 這將重新載入dataTable的資料
                            } else {
                                Swal.fire({
                                    icon: 'error',
                                    title: '錯誤',
                                    text: response.message
                                });
                            }
                        },
                        error: function (xhr, status, error) {
                            Swal.fire({
                                icon: 'error',
                                title: '錯誤',
                                text: "An error occurred: " + xhr.responseText
                            });
                        }
                    });
                } catch (error) {
                    console.error('Error:', error);
                }
            });

        </script>
    }

